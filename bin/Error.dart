typedef ErrorHandler = void Function(String message, ErrorType type);

enum ErrorType {
  author, // Generated by a "TODO" note in the ink source
  warning, // Indicates a non-critical issue that should be addressed
  error, // Critical error that can't be recovered from
}
